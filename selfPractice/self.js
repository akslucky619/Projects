var input = document.getElementById('input-field');
var inputValue = input.value;

var sst = [];

function add() {
    for (let i = 0; i < sst.length; i++) {
        const element = sst[i];
        
    }
}